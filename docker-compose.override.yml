services:
  # In dev, run the API locally with `mise run dev` (air hot reload).
  # Disable api and migrate from the default compose up.
  api:
    profiles:
      - prod

  migrate:
    profiles:
      - prod
